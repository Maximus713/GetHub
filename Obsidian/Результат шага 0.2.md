**ПРИСТУПАЮ К ФАЗЕ 0!**

## **ШАГ 0.1: АНАЛИЗ ОБЩЕГО ТЗ**

Из общего ТЗ выделяю **ТОЛЬКО архитектурные элементы**:

### **ФАЙЛОВАЯ СТРУКТУРА (9 файлов):**
```
npk-calculators/
├── npk-calculators.php                      # 1
├── shared/config/npk-database.php          # (существует)
└── module-concentration/
    ├── concentration-module.php            # 2
    ├── includes/
    │   ├── class-calculation-engine.php    # 3
    │   ├── class-ajax-handler.php          # 4
    │   ├── class-data-adapter.php          # 5
    │   └── class-shortcode.php             # 6
    ├── assets/
    │   ├── js/calculator.js                # 7
    │   └── css/calculator.css              # 8
    └── templates/
        └── calculator-template.php         # 9
```

### **КЛАССЫ (5 классов):**
1. `NPK_Calculators` (синглтон, главный файл плагина)
2. `NPK_Concentration_Module` (синглтон, модуль концентрации)
3. `Calculation_Engine` (статический, только логика)
4. `Data_Adapter` (синглтон, подготовка данных для UI)
5. `AJAX_Handler` (синглтон, обработка AJAX)
6. `Shortcode` (синглтон, рендеринг HTML)

### **МЕТОДЫ (сигнатуры):**

**Из п.4.4:**
```php
// Calculation_Engine
public static function calculate(
    int $fertilizer_id,
    float $concentration,
    float $water_volume,
    string $ground_type
): array
```

**Из п.7.3.1 (Data_Adapter):**
```php
public function get_ui_data(): array
// Возвращает структуру для wp_localize_script()
```

**AJAX обработчик:**
```php
// AJAX_Handler
public function handle_calculation(): void
```

**Шорткод:**
```php
// Shortcode
public function render($atts): string
```

### **WORDPRESS ХУКИ (из п.11.1):**
1. `plugins_loaded` → `NPK_Calculators::get_instance()`
2. `wp_enqueue_scripts` (приоритет 1) → регистрация скриптов
3. `init` → регистрация шорткода
4. `wp_ajax_nopriv_npk_concentration_calculate` → AJAX
5. `wp_ajax_npk_concentration_calculate` → AJAX

### **ФОРМАТЫ ДАННЫХ:**

**Для JavaScript (п.7.3.1):**
```javascript
window.NPK_Concentration_Data = {
    constants: {...},
    fertilizers: [...],
    profiles: {...},
    defaults: {...},
    ajax: {url: '', nonce: ''}
}
```

**AJAX запрос (п.7.3.2):**
```json
{
    "action": "npk_concentration_calculate",
    "fertilizer_id": 1,
    "concentration": 0.3,
    "water_volume": 10.0,
    "ground_type": "wet"
}
```

**AJAX ответ (п.7.3.3):**
```json
{
    "success": true,
    "data": {...}
}
```

---

## **ШАГ 0.2: СОЗДАНИЕ ТЗ-1**

**Файл создан после обращения к Техническому Заданию. Все требования ТЗ в файле соблюдены!**

```markdown
# ТЗ-1: АРХИТЕКТУРНЫЙ СКЕЛЕТ PLUGIN NPK CALCULATORS
## (ТОЛЬКО СТРУКТУРА, БЕЗ ЛОГИКИ)

## 1. ЦЕЛЬ
Создать ВСЕ файлы плагина с ПУСТЫМИ классами и методами.
Проверить базовую работоспособность архитектуры.

## 2. ТРЕБОВАНИЯ ИЗ ОСНОВНОГО ТЗ, КОТОРЫЕ ВКЛЮЧАЕМ:

### 2.1. Структура файлов (п.7.1)
Создать РОВНО 9 файлов в указанных папках.

### 2.2. Имена классов (п.3.2)
Создать РОВНО 6 классов с указанными именами.

### 2.3. Сигнатуры методов:
- `Calculation_Engine::calculate()` (п.4.4)
- `Data_Adapter::get_ui_data()` (п.7.3.1)
- `AJAX_Handler::handle_calculation()`
- `Shortcode::render()`

### 2.4. WordPress хуки (п.11.1)
Зарегистрировать ВСЕ указанные хуки в правильном порядке.

### 2.5. Форматы данных (п.7.3.1, 7.3.2, 7.3.3)
Подготовить структуры данных (пустые массивы правильного формата).

## 3. ТРЕБОВАНИЯ, КОТОРЫЕ ИСКЛЮЧАЕМ ИЗ ТЗ-1:

### ❌ НЕ ВКЛЮЧАЕМ (оставим для следующих ТЗ):
  - Все формулы расчёта (п.4.1, 4.2, 4.3)
  - Логику определения state
  - Алгоритмы проверки диапазонов
  - Цветовую логику (п.6.4)
  - Валидацию ввода
  - Дебаунс и анимации
  - Бизнес-правила
  - UI логику в JavaScript
  - CSS стили (кроме базовой структуры)

## 4. КОНКРЕТНЫЕ ЗАДАНИЯ ДЛЯ КАЖДОГО ФАЙЛА:

### 4.1. npk-calculators.php
- Класс `NPK_Calculators` (синглтон)
- Метод `get_instance()`
- Конструктор загружает `npk-database.php`
- Конструктор создает экземпляр `NPK_Concentration_Module`
- Хук: `plugins_loaded` → `NPK_Calculators::get_instance()`

### 4.2. concentration-module.php
- Класс `NPK_Concentration_Module` (синглтон)
- Загружает 4 класса зависимостей
- Регистрирует скрипты на `wp_enqueue_scripts` с приоритетом 1
- Регистрирует шорткод на `init`
- Регистрирует AJAX обработчики
- Метод `localize_script_data()` (вызывает `Data_Adapter::get_ui_data()`)

### 4.3. includes/class-calculation-engine.php
- Класс `Calculation_Engine`
- Статический метод `calculate()` (возвращает пустой массив)
- Приватный метод `validate_solubility_total()` (заглушка)

### 4.4. includes/class-data-adapter.php
- Класс `Data_Adapter` (синглтон)
- Метод `get_ui_data()` (возвращает массив правильного формата)
- Массив должен содержать все ключи из п.7.3.1 (пустые значения)

### 4.5. includes/class-ajax-handler.php
- Класс `AJAX_Handler` (синглтон)
- Метод `handle_calculation()` (возвращает JSON с `success: true` и пустым `data`)
- Проверяет наличие поля `action` в `$_POST`

### 4.6. includes/class-shortcode.php
- Класс `Shortcode` (синглтон)
- Метод `render()` (возвращает HTML контейнер с уникальным ID)
- Вызывает `wp_enqueue_script()` для скрипта калькулятора

### 4.7. templates/calculator-template.php
- HTML разметка по макету (п.5)
- Все элементы с правильными CSS классами (префикс `.npk-`)
- Все элементы с `data-` атрибутами для JavaScript
- Уникальный ID контейнера через `uniqid()`

### 4.8. assets/js/calculator.js
- Заглушка: `console.log('Calculator loaded')`
- Структура: класс `NPKCalculator` с методом `init()`
- Обработчик события `DOMContentLoaded`

### 4.9. assets/css/calculator.css
- Базовые стили для контейнера `.npk-calculator`
- CSS переменные для цветов (пустые)
- Медиа-запросы для адаптивности

## 5. СВЯЗИ МЕЖДУ ФАЙЛАМИ (ОБЯЗАТЕЛЬНЫЕ):

### 5.1. require_once пути:
```
npk-calculators.php → concentration-module.php
concentration-module.php → includes/*.php (4 файла)
concentration-module.php → templates/calculator-template.php
```

### 5.2. Вызовы между классами:
```
NPK_Concentration_Module → Data_Adapter::get_ui_data()
NPK_Concentration_Module → Shortcode::render()
AJAX_Handler → Calculation_Engine::calculate()
```

## 6. ПРОВЕРКИ ДЛЯ СКЕЛЕТА:

### 6.1. После создания всех файлов проверить:
1. ✅ WordPress видит плагин (активируется без ошибок)
2. ✅ Все классы загружаются (нет фатальных ошибок)
3. ✅ Шорткод `[npk_concentration]` работает (возвращает HTML)
4. ✅ Скрипты регистрируются (видны в `wp_scripts()`)
5. ✅ AJAX endpoint отвечает (JSON с `success: true`)

### 6.2. Архитектурные проверки:
1. ✅ Data_Adapter НЕ вызывает Calculation_Engine
2. ✅ Shortcode НЕ вызывает Calculation_Engine
3. ✅ JavaScript НЕ содержит бизнес-логику
4. ✅ Все селекторы CSS начинаются с `.npk-`

## 7. ФОРМАТ ВЫВОДА КАЖДОГО ФАЙЛА:

Перед кодом каждого файла выводить:

```
// ============================================
// ФАЙЛ: [имя_файла]
// АРХИТЕКТУРНАЯ РОЛЬ: [роль из ТЗ]
// ЗАВИСИМОСТИ: [список файлов]
// СВЯЗИ: [кто вызывает методы этого класса]
// ============================================
```

## 8. КРИТИЧЕСКИЕ ТРЕБОВАНИЯ ДЛЯ СКЕЛЕТА:

### 8.1. Безопасность:
- Все файлы проверяют `defined('ABSPATH')`
- Все публичные методы имеют type hints
- AJAX проверяет nonce (заглушка)

### 8.2. WordPress стандарты:
- Скрипты регистрируются с правильными депенденси
- Шорткод имеет правильный формат `[shortcode]`
- AJAX использует правильные actions

### 8.3. Архитектурная чистота:
- Классы имеют одну ответственность
- Нет циклических зависимостей
- Форматы данных соответствуют ТЗ

## 9. ЧТО СЧИТАТЬ УСПЕШНЫМ ВЫПОЛНЕНИЕМ ТЗ-1:

✅ Созданы все 9 файлов
✅ Все классы можно загрузить без ошибок
✅ WordPress активирует плагин без фатальных ошибок
✅ Шорткод возвращает HTML
✅ AJAX endpoint возвращает JSON
✅ Архитектурные связи соответствуют схеме

---

**ТЗ-1 готово!** Это чистый архитектурный скелет без логики.

## **СЛЕДУЮЩИЙ ШАГ:**

Теперь я буду создавать файлы **один за другим** по этому ТЗ-1. Начну с `npk-calculators.php`.

**Начинаем создание скелета?**