
В чем есть проблема. Когда я даю чату задание проверить файлы и найти ошибки, то чат ищет ошибки "случайным" образом. Он находит ошибки, но лишь часть. А часть пропускает. если повторить команду на поиск ошибок, чат еще раз пройдет по всем файлам, и опять найдет какие-то ошибки, но уже другие и опять не все.

Задача, нужно придумать принцип или алгоритм, по которому чат будет внимательно просматривать ВЕСЬ код и выявлять ВСЕ доступные ему ошибки.

Вторая проблема. Чат часто принимает за ошибки нормально работающие фрагменты кода и изменяет их.
Задача, нужно придумать принцип или алгоритм, по которому чат будет изменять код только в том случае, если там на самом деле есть ошибка. 

Напиши, как ты понял задачу?



Опишу оставшиеся проблемы: 

- Нет цветного слайдера слева от ползунка.
- - При каждой манипуляции с калькулятором перезагружается браузер. На 1-2 сек. появляется светлый экран и индикатор загрузки. Такого быть не должно. Все вычисления должны происходить быстро и незаметно. 
Это может быть связано с этой ошибкой?

## Чтобы экран не моргал
# 🎯 **ПАМЯТКА: Предотвращение "морганий" и подвисаний UI**

## **📌 ПРИЧИНА ПРОБЛЕМЫ:**

**"Синхронные AJAX запросы блокируют интерфейс"**

---

## **🔍 КОРЕНЬ ПРОБЛЕМЫ:**

### **Было (плохо):**

javascript

// При каждом изменении поля - сразу AJAX
input.addEventListener('change', () => {
    // 1. Блокировка UI
    // 2. AJAX запрос (300-500мс)
    // 3. Ожидание ответа
    // 4. Обновление ВСЕЙ страницы
    sendAjaxRequest();
});

**Результат:** Белый экран, подвисание на 0.5-1 секунду

---

## **✅ РЕШЕНИЕ (как должно быть):**

### **1. РАЗДЕЛЕНИЕ ЛОГИКИ:**

text

[Пользователь меняет значение]
    ↓
[Мгновенное обновление UI] ← requestAnimationFrame
    ↓
[Локальный расчёт] ← без AJAX, только JS
    ↓
[Отложенный AJAX] ← с debounce 300мс

### **2. КЛЮЧЕВЫЕ ТЕХНИКИ:**

#### **а) Дебаунс (Debounce)**

javascript

// ПРАВИЛЬНО:
let timeout;
input.addEventListener('input', () => {
    clearTimeout(timeout);
    timeout = setTimeout(() => {
        sendAjaxRequest(); // Только через 300мс после окончания ввода
    }, 300);
});

#### **б) Локальные расчёты**

javascript

// ВСЕ формулы должны дублироваться в JavaScript
function calculateMassLocal(concentration, volume, solubility) {
    // Та же формула, что на сервере
    return (concentration / 100) * volume * 1000 * (100 / solubility);
}

#### **в) Отмена предыдущих запросов**

javascript

let controller = null;

async function sendRequest() {
    if (controller) controller.abort(); // Отменяем старый
    controller = new AbortController();
    
    fetch(url, { signal: controller.signal });
}

#### **г) Анимации через requestAnimationFrame**

javascript

// ПЛОХО:
element.style.width = '50%';

// ХОРОШО:
requestAnimationFrame(() => {
    element.style.transition = 'width 0.15s ease';
    element.style.width = '50%';
});



## Чтобы не было просто шорт кода 
## **📌 ГЛАВНОЕ ПРАВИЛО:**

**"Что используется — должно быть загружено раньше"**

---

## **🔢 ПОРЯДОК ЗАГРУЗКИ (сверху вниз):**

text

1. КОНФИГУРАЦИЯ И БАЗЫ ДАННЫХ
   (npk-database.php, config.php)

2. БАЗОВЫЕ КЛАССЫ И ХЕЛПЕРЫ
   (helpers.php, utilities.php)

3. ЯДРО ЛОГИКИ
   (Calculation_Engine.php)

4. МОДУЛИ И КОМПОНЕНТЫ
   (Data_Adapter.php, AJAX_Handler.php)

5. ИНТЕРФЕЙС И ШОРТКОДЫ
   (Shortcode.php, templates/)
