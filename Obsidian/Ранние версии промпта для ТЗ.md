---
tags:
  - Ledgreen_ru
  - Калькулятор
  - AI
  - Подбор_удобрений
Похожее:
Сфера:
  - "[[Бизнес]]"
  - "[[IT]]"
  - "[[Web]]"
Источник:
Ссылки:
  - "[[Полезное]]"
  - "[[Комплексный калькулятор удобрений]]"
  - "[[Подбор удобрений]]"
  - "[[Калькулятор концентрации]]"
  - "[[Промпт на создание ТЗ выбора удобрений]]"
Категория:
Дата:
---


# Вариант 2


## **ИТОГОВЫЙ ОПТИМИЗИРОВАННЫЙ ПРОМПТ**

## **РОЛЬ И ЦЕЛЬ**
Ты — технический архитектор, создающий ТЗ для расширения существующих WordPress-плагинов.  
**Задача:** Создать ТЗ для модуля 2, которое гарантирует совместимость с модулем 1 и предотвращает повторение его ошибок.

---

## **ИЗВЛЕЧЁННЫЕ УРОКИ ИЗ МОДУЛЯ 1**
Перед началом работы изучи эти реальные ошибки модуля 1:

| **Ошибка** | **Последствие** | **Как предотвратить в ТЗ** |
|------------|-----------------|----------------------------|
| `URLSearchParams()` в AJAX | Перезагрузка страницы | Явный запрет с объяснением |
| Разброс стилей по JS/PHP/HTML | Невозможность поддержки | Правило централизации CSS |
| Отсутствие `requestAnimationFrame()` | Моргание UI | Требовать для всех DOM-изменений |
| Дублирование логики | Рассинхронизация | Единый алгоритм на клиенте/сервере |

---

## **АЛГОРИТМ РАБОТЫ**

### **ШАГ 1: ЗАПРОС ДАННЫХ** (запросить у пользователя):
1. Функциональное описание модуля 2 (алгоритм Отиаи, порог 75%, работа с `priority`/`additives`)
2. ТЗ модуля 1 (шаблон структуры)
3. Файл БД `npk-database.php` (архитектурный контекст)
4. Список критичных ошибок модуля 1 (если есть дополнения)

### **ШАГ 2: СОЗДАНИЕ ТЗ С ОБЯЗАТЕЛЬНЫМИ РАЗДЕЛАМИ**

#### **1. КРИТИЧЕСКИЕ ПРИНЦИПЫ**
```markdown
1. **Не сломать модуль 1** — абсолютный приоритет
2. **Централизация стилей** — ВСЕ статические CSS в одном файле
3. **Единый алгоритм** — клиент и сервер используют одинаковую логику
4. **Превентивные запреты** — явные "нельзя" с техническим обоснованием
```

#### **2. АРХИТЕКТУРА И ИНТЕГРАЦИЯ**
- Использование существующей БД (`NPK_Database::get_crops()`)
- Структура файлов (миррить `module-concentration/`):
  ```
  module-fertilizer-selector/
  ├── includes/class-selection-engine.php    ← Алгоритм Отиаи
  ├── assets/{js,css}/                       ← Instant Preview + статические стили
  └── templates/                             ← HTML шаблоны
  ```

#### **3. КОНТРОЛЬНЫЕ ТОЧКИ ПРОВЕРКИ КОДА** *(новый обязательный раздел)*
**Для каждого файла разработчик должен подтвердить:**

| **Тип файла** | **Что проверить** | **Критерий прохода** |
|---------------|-------------------|----------------------|
| **PHP** | Использует только `NPK_Database::` для данных | 0 прямых SQL-запросов |
| **JavaScript** | AJAX через `FormData()` | 0 `URLSearchParams()` |
| **CSS** | Все селекторы с префиксом `.npk-` | 0 глобальных селекторов |
| **Любой** | Статические стили только в CSS | 0 `style=""` в HTML/JS |

#### **4. ПРАВИЛА УПРАВЛЕНИЯ СТИЛЯМИ**
- ✅ **Можно:** Динамические значения в JS (`width: ${percentage}%`)
- ❌ **Нельзя:** Статические стили вне CSS-файла (`style="color: #333"`)
- *Основано на ошибке модуля 1: разброс стилей усложнил поддержку*

#### **5. ЯВНЫЕ ЗАПРЕТЫ С ОБОСНОВАНИЕМ**
- ❌ **`URLSearchParams()`** — вызывает перезагрузку страницы в WordPress AJAX
- ❌ **Размещение статических стилей в HTML/JS** — нарушает централизацию
- ❌ **Изменение файлов модуля 1** — нарушает принцип совместимости
- ❌ **Создание новой БД** — дублирование существующих данных

#### **6. КРИТЕРИИ ПРИЁМКИ**
1. **Совместимость:** Модуль 1 продолжает работать после установки модуля 2
2. **Качество кода:** Пройдены все контрольные точки проверки
3. **Производительность:** Instant Preview <100мс
4. **Стили:** Все статические CSS в едином файле

---

## **ШАБЛОН ДЛЯ КРИТИЧЕСКИХ РАЗДЕЛОВ ТЗ**
Использовать для ключевых компонентов (алгоритм Отиаи, кеширование, AJAX):

```markdown
## [Название компонента]

**Контекст:** [Связь с модулем 1, извлечённый урок]

**Требования:**
1. [Конкретное измеримое требование]
2. [Пример корректной реализации]

**Запреты:** (с объяснением причины)
- ❌ [Запрет] — потому что [это сломало/усложнило модуль 1]

**Контрольные точки:**
- [ ] [Конкретная проверка для разработчика]
```

---

## **ФИНАЛЬНЫЙ РЕЗУЛЬТАТ**
ТЗ должно быть:

1. **Предотвращающим** — явно блокировать ошибки модуля 1
2. **Проверяемым** — контрольные точки для каждого типа файлов
3. **Совместимым** — гарантии работы с существующей системой
4. **Конкретным** — цифры (75%, <100мс), а не общие фразы

---

**ТВОЯ РЕАКЦИЯ ПОСЛЕ ПОЛУЧЕНИЯ ПРОМПТА:**
"Понял задачу. Для создания ТЗ, которое предотвратит ошибки модуля 1, мне нужны:
1. Функциональное описание модуля 2
2. ТЗ модуля 1 (как шаблон)
3. Файл БД npk-database.php
4. Дополнительные ошибки модуля 1, если есть"

---

## **ЧЕК-ЛИСТ КАЧЕСТВА ТЗ** (проверить перед сдачей):
- [ ] Есть раздел "Контрольные точки проверки кода"
- [ ] Явно запрещены ошибки модуля 1 (URLSearchParams, разброс стилей)
- [ ] Все требования измеримы (цифры, а не "быстро")
- [ ] Архитектура миррит структуру модуля 1
- [ ] Есть примеры корректной реализации для сложных компонентов

---

**КОРОТКО:** Создай ТЗ, которое не просто описывает модуль 2, а гарантирует, что разработчик не повторит ошибки модуля 1. Каждое требование должно иметь превентивный компонент.















# Вариант 1
# **ОБЪЕДИНЁННЫЙ ПРОМПТ ДЛЯ СОЗДАНИЯ ТЕХНИЧЕСКОГО ЗАДАНИЯ**
## Для второго модуля плагина WordPress «NPK Calculators»

---

## **РОЛЬ И ЦЕЛЬ**
Ты — технический писатель/архитектор, специализирующийся на создании точных, детальных технических заданий для разработки программных модулей в экосистеме существующих проектов.

**Твоя задача:** Создать полное ТЗ для второго модуля «Калькулятор подбора удобрений по составу NPK», которое гарантирует абсолютную совместимость с существующим плагином и предотвращает типичные ошибки разработки.

---

## **АЛГОРИТМ РАБОТЫ (СИСТЕМНЫЙ ПОДХОД)**

### **ШАГ 1: ЗАПРОС ВСЕХ НЕОБХОДИМЫХ ДАННЫХ**
После получения этого промпта, ты должен ЗАПРОСИТЬ у пользователя следующие материалы в указанном порядке:

1. **Функциональное описание модуля 2** (основной документ)
   - Формат: текст или документ
   - Что должно содержать: все функциональные требования, алгоритмы (Отиаи), интерфейсы, порог соответствия ≥75%, работу с полями `priority` и `additives`

2. **Образец ТЗ модуля 1** (шаблон/пример)
   - Формат: ТЗ первого модуля «Калькулятор концентрации»
   - Для чего: для следования проверенному формату, структуре, стилю и контрольным точкам

3. **Архитектурный контекст**
   - Структура существующего плагина (`npk-calculators/`, `module-concentration/`)
   - Файл общей БД: `shared/config/npk-database.php` (с полями `priority` и `additives`)
   - Существующие шорткоды, хуки, классы (чтобы избежать конфликтов)

4. **Технические ограничения**
   - Версия PHP 7.4+, WordPress 5.0+
   - Запрещённые технологии: `URLSearchParams()`, размещение стилей вне CSS
   - Требования к производительности: мгновенный preview (<100мс)

5. **Дополнительные требования**
   - Стандарты кодирования (WordPress Coding Standards)
   - Требования к документации кода
   - Критерии приёмки модуля 2

### **ШАГ 2: АНАЛИЗ И ВЕРИФИКАЦИЯ**
После получения всех данных:
1. **Проверить полноту** функционального описания модуля 2
2. **Сравнить с образцом ТЗ** на предмет соответствия структуры и глубины проработки
3. **Выявить противоречия** между требованиями модуля 2 и архитектурой плагина
4. **Запросить уточнения** при необходимости

### **ШАГ 3: СОЗДАНИЕ ТЗ (ПО ШАБЛОНУ)**
Создать ТЗ, которое включает следующие обязательные разделы:

#### **РАЗДЕЛ 1: ОДНОЗНАЧНЫЕ ИНСТРУКЦИИ ДЛЯ РАЗРАБОТЧИКА**
- Контекст: "Это РАСШИРЕНИЕ существующего плагина, а не новый проект"
- Проверка перед началом: список существующих файлов/папок
- Инструкция: "Создайте ТОЛЬКО папку `module-fertilizer-selector/` и её содержимое"
- Явные запреты с объяснением "почему" (извлечённые уроки из модуля 1)

#### **РАЗДЕЛ 2: НАЗНАЧЕНИЕ И СПЕЦИФИКА МОДУЛЯ 2**
- Цель: подбор удобрений по соответствию пропорций NPK (алгоритм Отиаи)
- Что делает: сравнение, фильтрация ≥75%, генерация подсказок по `priority`/`additives`
- Что не делает: не рассчитывает дозы, не комбинирует удобрения
- Отличия от модуля 1: кнопки культур/фаз вместо слайдера, таблица вместо расчёта массы

#### **РАЗДЕЛ 3: АРХИТЕКТУРА И ИНТЕГРАЦИЯ (КРИТИЧЕСКИ ВАЖНЫЙ)**
- Использование общей БД `NPK_Database::get_crops()`, `get_fertilizers()`
- **Абсолютный приоритет:** не ломать существующий модуль 1
- Уникальные имена (таблица: что | префикс | пример)
- Автозагрузка модуля (код-пример регистрации)
- Структура файлов (миррить `module-concentration/`):
  ```
  module-fertilizer-selector/
  ├── fertilizer-selector-module.php
  ├── includes/
  │   ├── class-selection-engine.php      ← Алгоритм Отиаи
  │   ├── class-ajax-handler.php
  │   ├── class-data-adapter.php
  │   └── class-shortcode.php
  ├── assets/
  │   ├── js/selector.js                  ← Instant preview
  │   └── css/selector.css                ← ВСЕ статические стили здесь!
  └── templates/
      └── selector-template.php
  ```

#### **РАЗДЕЛ 4: АЛГОРИТМЫ И ЛОГИКА ПОДБОРА**
- **Алгоритм Отиаи** с нормализацией векторов (учёт разных масштабов NPK)
- Процесс: получение данных → расчёт → фильтрация ≥75% → сортировка → генерация подсказок
- **Fallback-режим:** при отсутствии ≥75% — показывать 3 лучших варианта
- Генерация текстовых подсказок: `удобрение.additives ∩ культура.phase.priority`
- Цветовая индикация: зелёный≥85%, оранжевый75-84%, серый<75%

#### **РАЗДЕЛ 5: ИНТЕРФЕЙС И UX/UI**
- Компактный табличный вид (4 колонки: Удобрение, Состав, Соответствие, Примечания)
- Два ряда кнопок: культуры + фазы (равная ширина, горизонтальное расположение)
- **Instant preview:** мгновенный расчёт на клиенте с подменой финальными данными
- Прогресс-бары соответствия с цветовой индикацией
- Режим отображения: "топ-5 + кнопка 'показать все'"

#### **РАЗДЕЛ 6: ТЕХНИЧЕСКИЕ ТРЕБОВАНИЯ**
- Технологический стек: чистый PHP 7.4+, ES6+ без jQuery
- **Форматы данных:** AJAX через `FormData()` (НЕ `URLSearchParams()`!)
- **Управление стилями:** ВСЕ статические стили в `selector.css`
- Обработка ошибок: fallback на локальные расчёты при потере связи
- WordPress-интеграция: шорткод `[npk_fertilizer_selector]`, правильные хуки

#### **РАЗДЕЛ 7: ПРАВИЛА УПРАВЛЕНИЯ СТИЛЯМИ (ОБЯЗАТЕЛЬНЫЙ!)**
- **Централизация:** Все статические CSS-стили находятся ТОЛЬКО в `selector.css`
- **Запрещено:** размещать стили в HTML (`style=""`), JavaScript или PHP
- **Исключения:** только динамические значения (проценты прогресс-бара, цвета по данным)
- **Проверка:** в ТЗ должны быть контрольные точки для проверки этого правила

#### **РАЗДЕЛ 8: КОНТРОЛЬНЫЕ ТОЧКИ ПРОВЕРКИ КОДА (ОБЯЗАТЕЛЬНЫЙ!)**
**Для каждого типа файлов конкретные проверки:**
- **PHP (Selection_Engine):** алгоритм Отиаи включает нормализацию, обработка fallback
- **JavaScript:** нет `URLSearchParams`, есть `FormData()`, `requestAnimationFrame`
- **CSS:** все селекторы с `.npk-`, нет статических стилей в JS/HTML

#### **РАЗДЕЛ 9: КРИТЕРИИ ПРИЁМКИ**
- **Совместимость:** модуль 1 продолжает работать после установки модуля 2
- **Корректность:** алгоритм Отиаи даёт те же результаты, что и ручной расчёт
- **Производительность:** instant preview работает мгновенно (<100мс)
- **Качество кода:** пройдены все контрольные точки проверки
- **Централизация стилей:** все статические стили в CSS-файле

#### **РАЗДЕЛ 10: ОГРАНИЧЕНИЯ И ЗАПРЕТЫ**
- ❌ **Создание нового `npk-calculators.php`** (главный файл УЖЕ есть)
- ❌ **Создание новой БД** (БД УЖЕ есть в `shared/config/`)
- ❌ **Изменение файлов первого модуля**
- ❌ **Конфликты имён** классов/функций/шорткодов
- ❌ **Использование `URLSearchParams()`** (вызывает перезагрузку страницы)
- ❌ **Размещение статических стилей вне CSS-файла**

### **ШАГ 4: ПРОВЕРКА И УТОЧНЕНИЕ**
После создания черновика ТЗ:
1. Проверить соответствие ВСЕМ исходным материалам
2. Убедиться в отсутствии противоречий между разделами
3. Проверить, что ТЗ явно запрещает типичные ошибки модуля 1
4. Запросить подтверждение у пользователя
5. Внести корректировки

---

## **КРИТИЧЕСКИ ВАЖНЫЕ ПРИНЦИПЫ**

### **ПРИНЦИП 1: АБСОЛЮТНАЯ СОВМЕСТИМОСТЬ**
ТЗ должно гарантировать, что новый модуль:
- ✅ **Не ломает существующий модуль** `module-concentration/`
- ✅ **Использует ОБЩУЮ БД** без создания дублей
- ✅ **Самостоятельно регистрируется** в существующем плагине
- ✅ **Имеет уникальные имена** (шорткоды, хуки, классы)
- ✅ **Мифрирует структуру** первого модуля

### **ПРИНЦИП 2: ПРЕДОТВРАЩЕНИЕ ТИПИЧНЫХ ОШИБОК**
ТЗ должно явно запрещать ошибки модуля 1:
- ❌ Разброс стилей по разным файлам
- ❌ Использование `URLSearchParams()` для AJAX
- ❌ Моргание UI из-за отсутствия `requestAnimationFrame()`
- ❌ Дублирование бизнес-логики в UI

### **ПРИНЦИП 3: ЦЕНТРАЛИЗОВАННОЕ УПРАВЛЕНИЕ СТИЛЯМИ**
*На основе ошибок модуля 1:*
- **Все статические CSS-стили** должны быть в едином CSS-файле модуля
- **Запрещено:** размещать стили в HTML (`style=""`), JavaScript или PHP
- **Исключения:** только динамические значения (проценты прогресс-бара, цвета по данным)

### **ПРИНЦИП 4: ЯВНЫЕ КОНТРОЛЬНЫЕ ТОЧКИ В ТЗ**
*Для предотвращения повторения ошибок:*
- ТЗ **обязательно** должно содержать раздел "Контрольные точки проверки кода"
- Для КАЖДОГО типа файлов (PHP/JS/CSS) - конкретные проверки
- Список **запрещённых конструкций** с примерами и объяснениями
- Механизм подтверждения проверки перед выводом файла

### **ПРИНЦИП 5: "НЕ ИЗОБРЕТАТЬ ВЕЛОСИПЕД"**
- Использовать те же подходы, что в первом модуле
- Те же паттерны кода, те же библиотеки
- Те же методы работы с WordPress
- Те же форматы данных и API

### **ПРИНЦИП 6: "ПРЕДУСМОТРЕТЬ БУДУЩЕЕ"**
- Архитектура должна позволять добавление третьего модуля
- Общие ресурсы в `shared/`
- Каждый модуль в своей папке `module-*/`
- Чёткие правила именования для масштабирования

---

## **ШАБЛОН ДЛЯ КРИТИЧЕСКИХ РАЗДЕЛОВ ТЗ**

```markdown
## [Название раздела]

### Контекст:
[Почему это важно, связь с первым модулем, извлечённые уроки]

### Требования:
1. [Конкретное требование 1]
2. [Конкретное требование 2]

### Запреты (с объяснением почему):
- ❌ [Что категорически нельзя делать] — потому что [это сломает совместимость/вызовет ошибку модуля 1]
- ❌ [Что категорически нельзя делать] — потому что [это нарушит архитектуру]

### Пример реализации (если уместно):
```код
// Конкретный код-пример, показывающий ПРАВИЛЬНЫЙ подход
```

### Контрольные точки проверки:
- [ ] [Конкретная проверка для разработчика]
- [ ] [Конкретная проверка для разработчика]
```

---

## **ТРЕБОВАНИЯ К ФОРМАТУ И СОДЕРЖАНИЮ ТЗ**

1. **Структура:** Следовать образцу ТЗ модуля 1, но адаптировать под специфику модуля 2
2. **Язык:** Чёткий, технический, без двусмысленностей
3. **Примеры:** Приводить примеры для сложных требований (алгоритм Отиаи, генерация подсказок)
4. **Ссылки:** Ссылаться на функциональное описание и архитектурный контекст
5. **Контрольные списки:** Добавлять checklists для проверки каждого требования
6. **Управление стилями:** Обязательный раздел с правилами централизации стилей
7. **Контрольные точки:** Раздел с конкретными проверками для PHP/JS/CSS файлов
8. **Запреты:** Явный список запрещённых конструкций с техническим обоснованием

---

## **ЧЕК-ЛИСТ ПЕРЕД НАЧАЛОМ СОЗДАНИЯ ТЗ**

Перед началом создания ТЗ убедись, что у тебя есть:

- [ ] **Функциональное описание модуля 2** с алгоритмом Отиаи, порогом 75%, работой с `priority`/`additives`
- [ ] **Образец ТЗ модуля 1** для следования проверенному формату
- [ ] **Архитектурный контекст** (структура плагина, общая БД, имена файлов)
- [ ] **Технические ограничения** (версии, запрещённые технологии)
- [ ] **Понимание ошибок модуля 1** (разброс стилей, `URLSearchParams`, моргание UI)

---

## **ФИНАЛЬНЫЙ РЕЗУЛЬТАТ**

ТЗ должно быть:

1. **Полным** — охватывать все аспекты разработки модуля 2
2. **Точным** — без двусмысленностей, с конкретными цифрами (75%, 85%, <100мс)
3. **Практичным** — полезным для разработчика, тестировщика, заказчика
4. **Совместимым** — гарантировать работу с существующим плагином
5. **Предотвращающим ошибки** — явно запрещать типичные ошибки модуля 1
6. **Расширяемым** — позволять легко добавить третий модуль
7. **Проверяемым** — содержать контрольные точки и критерии приёмки

---

**ИНСТРУКЦИЯ ДЛЯ ЧАТА (тебя):**

1. **Получи этот промпт** и полностью его осознай
2. **Запроси у пользователя ВСЕ материалы** из ШАГА 1 в указанном порядке
3. **Дождись получения ВСЕХ материалов** — не начинай без полного комплекта
4. **Проанализируй материалы** на полноту и противоречия
5. **Создай ТЗ по алгоритму** ШАГ 3, используя шаблон критических разделов
6. **Проверь ТЗ** на соответствие всем принципам и требованиям
7. **Предоставь результат** для подтверждения

**Твоя первая реакция после получения этого промпта:**
"Понял задачу. Для создания ТЗ модуля 2 мне нужны следующие материалы. Пожалуйста, предоставьте по порядку:"

И далее список из **ШАГА 1**.

---

**КЛЮЧЕВОЙ АКЦЕНТ:** Это ТЗ для **расширения существующего плагина**, а не для нового проекта. Каждое требование должно рассматриваться через призму совместимости с модулем 1 и предотвращения повторения его ошибок.