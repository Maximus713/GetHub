---
tags:
  - Ledgreen_ru
  - Калькулятор
  - AI
  - Подбор_удобрений
Похожее:
Сфера:
  - "[[Бизнес]]"
  - "[[IT]]"
  - "[[Web]]"
Источник:
Ссылки:
  - "[[Полезное]]"
  - "[[Комплексный калькулятор удобрений]]"
  - "[[Подбор удобрений]]"
  - "[[Калькулятор концентрации]]"
  - "[[Схема_5 Поэтапного создания плагина]]"
Категория:
Дата:
---




ПАКЕТ ПРИНЯТ. ВЕРДИКТ АУДИТА ПРИНЯТ. НАЧИНАЮ ПЕРЕДЕКОМПОЗИЦИЮ СТРОГО ПО ЭТАЛОННОМУ ПОРЯДКУ...

═══════════════════════════════════════════════════════════════════════════════
🔄 ПОЛНАЯ ПЕРЕДЕКОМПОЗИЦИЯ: ЭТАЛОННЫЙ ПОРЯДОК №1 → №9
═══════════════════════════════════════════════════════════════════════════════

❌ СТАРАЯ НУМЕРАЦИЯ (ОТКЛОНЕНА):
   №3 Data_Adapter → требует №9 Cache_Manager (НО №9 ПОСЛЕ!) → ФАТАЛЬНАЯ ОШИБКА

✅ ЭТАЛОННАЯ НУМЕРАЦИЯ (СТРОГО):
   №1 fertilizer-selector-module.php
   №2 class-selection-engine.php
   №3 class-cache-manager.php         ← КЭШ РАНЬШЕ ЗАВИСИМЫХ
   №4 class-data-adapter.php          ← УЖЕ МОЖЕТ ИСПОЛЬЗОВАТЬ КЭШ
   №5 class-ajax-handler.php          ← УЖЕ МОЖЕТ ИСПОЛЬЗОВАТЬ КЭШ
   №6 class-shortcode.php
   №7 templates/selector-template.php
   №8 assets/css/selector.css
   №9 assets/js/selector.js

⚠️ ВСЕ ЗАВИСИМОСТИ ЗАМКНУТЫ:
   • №4 (Data_Adapter) → №3 (Cache_Manager) — ✅ СУЩЕСТВУЕТ ДО
   • №5 (AJAX_Handler) → №3 (Cache_Manager) — ✅ СУЩЕСТВУЕТ ДО
   • №5 (AJAX_Handler) → №2 (Selection_Engine) — ✅ СУЩЕСТВУЕТ ДО
   • №6 (Shortcode) → №4 (Data_Adapter) — ✅ СУЩЕСТВУЕТ ДО
   • №9 (JS) → №5 (AJAX_Handler) — ✅ СУЩЕСТВУЕТ ДО

═══════════════════════════════════════════════════════════════════════════════

═══════════════════════════════════════════════════════════════════════════════
ЗАДАНИЕ №1: fertilizer-selector-module.php — Точка входа модуля
═══════════════════════════════════════════════════════════════════════════════

🎯 ЦЕЛЬ:
Создать главный файл модуля, который регистрирует модуль в ядре WordPress, подключает все зависимости и устанавливает хуки жизненного цикла. Без этого файла модуль не будет загружен.

📁 ФАЙЛЫ:
   СОЗДАТЬ: [module-fertilizer-selector/fertilizer-selector-module.php]

📋 ТРЕБОВАНИЯ:
1. Защита от прямого доступа: `defined('ABSPATH') || exit;`
2. Хук инициализации: `add_action('plugins_loaded', 'npk_fertilizer_selector_init', 5);`
3. Подключение всех классов в порядке зависимостей:
   ✅ `require_once __DIR__ . '/includes/class-selection-engine.php';`
   ✅ `require_once __DIR__ . '/includes/class-cache-manager.php';`    ← №3 в порядке создания
   ✅ `require_once __DIR__ . '/includes/class-data-adapter.php';`     ← №4
   ✅ `require_once __DIR__ . '/includes/class-ajax-handler.php';`     ← №5
   ✅ `require_once __DIR__ . '/includes/class-shortcode.php';`        ← №6
4. Регистрация классов в хуке инициализации
5. **НЕ** содержит enqueue скриптов/стилей — это ответственность Shortcode

✅ ЧТО МОЖНО:
   • `plugins_loaded` — приоритет 5
   • `register_activation_hook`, `register_deactivation_hook`
   • `require_once`
   • `defined('ABSPATH')`

⛔ ЗАПРЕТЫ:
❌ [Неправильные имена файлов] — только kebab-case
❌ [Подключение несуществующих файлов] — все require_once ведут на реальные файлы
❌ [Регистрация шорткода] — НЕТ `add_shortcode()` в этом файле

🧪 ЧЕК-ЛИСТ:
[ ] Защита от прямого доступа
[ ] Хук `plugins_loaded` с приоритетом 5
[ ] Подключены ВСЕ 5 классов в правильном порядке (сначала ядро и кэш)
[ ] Нет `add_shortcode()`
[ ] Нет SQL-запросов
[ ] Имена файлов в kebab-case

═══════════════════════════════════════════════════════════════════════════════

═══════════════════════════════════════════════════════════════════════════════
ЗАДАНИЕ №2: class-selection-engine.php — Ядро алгоритма Отиаи (КРИТИЧЕСКОЕ)
═══════════════════════════════════════════════════════════════════════════════

🎯 ЦЕЛЬ:
Создать центральный класс бизнес-логики модуля, реализующий алгоритм Отиаи. АБСОЛЮТНЫЙ ИСТОЧНИК ИСТИНЫ для всех расчётов.

📁 ФАЙЛЫ:
   СОЗДАТЬ: [module-fertilizer-selector/includes/class-selection-engine.php]
   КЛАСС: `NPK_Selection_Engine` (с префиксом NPK_)

📋 ТРЕБОВАНИЯ:
1. Алгоритм Отиаи: нормализация векторов → поэлементный минимум → сумма минимумов → ×100
2. Порог соответствия: **ОБЯЗАТЕЛЬНО** через `NPK_Database::get_constants()['thresholds']['min_match_percentage']`
   ❌ ЗАПРЕЩЕНО хардкодить 75
3. Fallback-режим: при отсутствии совпадений ≥ порога — 3 лучших с флагом `'fallback' => true`
4. Генерация подсказок: пересечение `fertilizer.additives` ∩ `crop.phase.priority`
5. Цветовая классификация: 
   - зелёный (≥85%) — `NPK_Database::get_constants()['colors']['green_threshold']`
   - оранжевый (75-84%) — `NPK_Database::get_constants()['colors']['orange_range']`
   - серый (<75% или fallback)
6. Метод: `public function find_matching_fertilizers(string $crop_id, string $phase_id): array`

✅ ЧТО МОЖНО:
   • `NPK_Database::get_fertilizers()`
   • `NPK_Database::get_crops()`
   • `NPK_Database::normalize_npk()`
   • `NPK_Database::get_constants()`
   • array_map, array_sum, arsort

⛔ ЗАПРЕТЫ:
❌ [Хардкод порогов] — НИКАКИХ чисел 75, 85 в теле методов
❌ [Дублирование логики] — алгоритм ТОЛЬКО здесь
❌ [Запрещённые вызовы] — НЕ вызывать Data_Adapter, AJAX_Handler, Shortcode
❌ [Неправильные имена] — файл `class-selection-engine.php`, класс `NPK_Selection_Engine`

🧪 ЧЕК-ЛИСТ:
[ ] Порог получен через `NPK_Database::get_constants()`
[ ] КТ-1: Азофоска [16,16,16] vs Томаты/цветение [1,1,2] → 83%
[ ] Fallback: ровно 3 результата, флаг `'fallback' => true`
[ ] Подсказки: только пересечение additives ∩ priority
[ ] Цвета: ключи `'color' => 'green'/'orange'/'grey'`
[ ] Нормализация через `NPK_Database::normalize_npk()`
[ ] Нет SQL-запросов
[ ] Нет чисел 75, 85 в коде (кроме комментариев)

⚠️ ТРИГГЕР АУДИТА: Алгоритм Отиаи + Константы порога

═══════════════════════════════════════════════════════════════════════════════

═══════════════════════════════════════════════════════════════════════════════
ЗАДАНИЕ №3: class-cache-manager.php — Кеширование результатов (НОВАЯ ПОЗИЦИЯ)
═══════════════════════════════════════════════════════════════════════════════

🎯 ЦЕЛЬ:
Создать класс для кеширования результатов Selection_Engine. ДОЛЖЕН БЫТЬ СОЗДАН РАНЬШЕ Data_Adapter и AJAX_Handler, так как они от него зависят.

📁 ФАЙЛЫ:
   СОЗДАТЬ: [module-fertilizer-selector/includes/class-cache-manager.php]
   КЛАСС: `NPK_Cache_Manager` (с префиксом NPK_)

📋 ТРЕБОВАНИЯ:
1. Кеширование результатов `NPK_Selection_Engine::find_matching_fertilizers()`
2. Время жизни кеша: 1 час (`HOUR_IN_SECONDS`)
3. Ключ кеша: `'npk_fert_rec_' . $crop_id . '_' . $phase_id`
4. Методы:
   - `public function get(string $key): mixed`
   - `public function set(string $key, $data, int $ttl = HOUR_IN_SECONDS): bool`
   - `public function delete(string $key): bool`
5. Использование WordPress Transients API

✅ ЧТО МОЖНО:
   • `set_transient()`, `get_transient()`, `delete_transient()`
   • `HOUR_IN_SECONDS`

⛔ ЗАПРЕТЫ:
❌ [Бизнес-логика в кеше] — НЕ вызывать `NPK_Selection_Engine::find_matching_fertilizers()`
❌ [Долгое кеширование] — ТОЛЬКО `HOUR_IN_SECONDS`
❌ [Неправильные имена] — файл `class-cache-manager.php`, класс `NPK_Cache_Manager`

🧪 ЧЕК-ЛИСТ:
[ ] Класс `NPK_Cache_Manager`
[ ] Префикс ключей: `protected $prefix = 'npk_fert_rec_';`
[ ] Метод `get()`: `get_transient($this->prefix . $key)`
[ ] Метод `set()`: `set_transient($this->prefix . $key, $data, $ttl)`
[ ] TTL по умолчанию: `HOUR_IN_SECONDS`
[ ] Нет вызовов `NPK_Selection_Engine`
[ ] КТ-7: Кеш = 1 час

⚠️ ТРИГГЕР АУДИТА: Cache_Manager — зависимость для Data_Adapter и AJAX_Handler

═══════════════════════════════════════════════════════════════════════════════

═══════════════════════════════════════════════════════════════════════════════
ЗАДАНИЕ №4: class-data-adapter.php — Подготовка данных для UI (ИСПРАВЛЕНО)
═══════════════════════════════════════════════════════════════════════════════

🎯 ЦЕЛЬ:
Создать адаптер данных, который извлекает справочники из БД/кеша и передаёт их в JavaScript. НИКАКОЙ логики расчётов.

📁 ФАЙЛЫ:
   СОЗДАТЬ: [module-fertilizer-selector/includes/class-data-adapter.php]
   КЛАСС: `NPK_Data_Adapter`

📋 ТРЕБОВАНИЯ:
1. Метод `public function get_localized_data(): array` для `wp_localize_script()`
2. **ОБЯЗАТЕЛЬНО**: использовать `NPK_Cache_Manager` для кеширования справочников
   - `$cache->get('all_crops')` → если нет, запросить из БД и `$cache->set()`
   - `$cache->get('all_fertilizers_minimal')` → аналогично
3. Получение данных ТОЛЬКО через `NPK_Database::`
4. **СТРОГО ЗАПРЕЩЕНО** вызывать `NPK_Selection_Engine`
5. **СТРОГО ЗАПРЕЩЕНО** участвовать в AJAX или расчётах

✅ ЧТО МОЖНО:
   • `NPK_Database::get_crops()`
   • `NPK_Database::get_fertilizers()`
   • `NPK_Cache_Manager::get()/set()`
   • `wp_parse_args()`

⛔ ЗАПРЕТЫ:
❌ [Участие в расчётах] — НЕТ вызовов `NPK_Selection_Engine`
❌ [Игнорирование кеша] — ОБЯЗАТЕЛЬНО использовать `NPK_Cache_Manager`
❌ [Передача данных в Calculation_Engine] — данные текут ТОЛЬКО БД → UI
❌ [Определение state/цветов] — НЕТ ключей 'state', 'color', 'status'

🧪 ЧЕК-ЛИСТ:
[ ] Подключение: `require_once __DIR__ . '/class-cache-manager.php';`
[ ] Создание экземпляра: `$cache = new NPK_Cache_Manager();`
[ ] Кеш для культур: `$crops = $cache->get('all_crops') ?? NPK_Database::get_crops();`
[ ] Сохранение в кеш: `if (null === $crops) { $cache->set('all_crops', $crops); }`
[ ] Возвращает массив с 'crops', 'fertilizers', 'ajax_url', 'nonce'
[ ] НЕТ вызовов `NPK_Selection_Engine::*`
[ ] КТ-6: Единственный выход — `wp_localize_script()`

⚠️ ТРИГГЕР АУДИТА: Data_Adapter — проверка границ (0 расчётов, только кеш + БД)

═══════════════════════════════════════════════════════════════════════════════

═══════════════════════════════════════════════════════════════════════════════
ЗАДАНИЕ №5: class-ajax-handler.php — Транспортный слой (ИСПРАВЛЕНО)
═══════════════════════════════════════════════════════════════════════════════

🎯 ЦЕЛЬ:
Создать обработчик AJAX-запросов. Принимает ID культуры/фазы, вызывает Selection_Engine, кеширует через Cache_Manager, возвращает JSON.

📁 ФАЙЛЫ:
   СОЗДАТЬ: [module-fertilizer-selector/includes/class-ajax-handler.php]
   КЛАСС: `NPK_AJAX_Handler`

📋 ТРЕБОВАНИЯ:
1. Хуки: `wp_ajax_npk_fertilizer_select` и `wp_ajax_nopriv_npk_fertilizer_select`
2. Получение данных: ТОЛЬКО `$_POST` (FormData) — НИКАКОГО JSON
3. Валидация: `check_ajax_referer('npk_fertilizer_nonce', 'nonce')`
4. Кеш: `NPK_Cache_Manager::get("rec_{$crop_id}_{$phase_id}")` — проверять ДО расчёта
5. Расчёт: `NPK_Selection_Engine::find_matching_fertilizers($crop_id, $phase_id)`
6. Ответ: `wp_send_json_success(['recommendations' => $results])`

✅ ЧТО МОЖНО:
   • `$_POST` — ТОЛЬКО этот суперглобальный
   • `check_ajax_referer()`
   • `NPK_Selection_Engine::find_matching_fertilizers()`
   • `NPK_Cache_Manager::get()/set()`
   • `wp_send_json_success()/wp_send_json_error()`

⛔ ЗАПРЕТЫ:
❌ [Неправильный формат AJAX] — ❌ НЕТ `php://input`, `json_decode`, `Content-Type: application/json`
❌ [Игнорирование кеша] — ОБЯЗАТЕЛЬНО проверять `NPK_Cache_Manager::get()` ДО расчёта
❌ [Запрещённые вызовы] — НЕ вызывать `NPK_Data_Adapter`
❌ [Бизнес-логика в транспорте] — НЕ рассчитывать ничего самостоятельно

🧪 ЧЕК-ЛИСТ:
[ ] Подключение: `require_once __DIR__ . '/class-cache-manager.php';`
[ ] Подключение: `require_once __DIR__ . '/class-selection-engine.php';`
[ ] Nonce проверка
[ ] Получение: ТОЛЬКО `$_POST['crop_id']`, `$_POST['phase_id']`
[ ] Кеш: `$cache = new NPK_Cache_Manager(); $cached = $cache->get("rec_{$crop_id}_{$phase_id}");`
[ ] Расчёт: только если `null === $cached`
[ ] Сохранение в кеш: `$cache->set("rec_{$crop_id}_{$phase_id}", $results);`
[ ] КТ-4: ответ = точное совпадение с ручным расчётом Selection_Engine

⚠️ ТРИГГЕР АУДИТА: AJAX — FormData, запрет JSON, кеширование

═══════════════════════════════════════════════════════════════════════════════

═══════════════════════════════════════════════════════════════════════════════
ЗАДАНИЕ №6: class-shortcode.php — Генерация контейнера
═══════════════════════════════════════════════════════════════════════════════

📁 ФАЙЛЫ:
   СОЗДАТЬ: [module-fertilizer-selector/includes/class-shortcode.php]
   КЛАСС: `NPK_Shortcode`

📋 ТРЕБОВАНИЯ:
1. Шорткод: `[npk_fertilizer_selector]`
2. Enqueue: `selector.css`, `selector.js` — ТОЛЬКО при вызове шорткода
3. Локализация: `$data = NPK_Data_Adapter::get_localized_data();`
4. Буферизация: `include __DIR__ . '/../templates/selector-template.php'`

✅ ЧТО МОЖНО:
   • `add_shortcode()`
   • `wp_enqueue_style()/script()`
   • `wp_localize_script()`
   • `NPK_Data_Adapter::get_localized_data()`
   • `ob_start()/ob_get_clean()`

⛔ ЗАПРЕТЫ:
❌ [Шорткод не работает] — регистрация ПОСЛЕ загрузки всех классов
❌ [Позднее подключение скриптов] — enqueue ТОЛЬКО внутри шорткода
❌ [Инлайн-стили] — НЕТ `style="..."` в HTML-выводе

🧪 ЧЕК-ЛИСТ:
[ ] `add_shortcode('npk_fertilizer_selector', [self::class, 'render'])`
[ ] `wp_enqueue_style('npk-fertilizer-selector', ...)`
[ ] `wp_enqueue_script('npk-fertilizer-selector', ..., ['jquery'])`
[ ] `wp_localize_script('npk-fertilizer-selector', 'NPK_Selector_Data', $data)`
[ ] Буферизация шаблона

═══════════════════════════════════════════════════════════════════════════════

═══════════════════════════════════════════════════════════════════════════════
ЗАДАНИЕ №7: selector-template.php — Чистый HTML-скелет
═══════════════════════════════════════════════════════════════════════════════

📁 ФАЙЛЫ:
   СОЗДАТЬ: [module-fertilizer-selector/templates/selector-template.php]

📋 ТРЕБОВАНИЯ:
1. ТОЛЬКО HTML + PHP для отрисовки (echo, foreach, if)
2. НИКАКОЙ логики расчётов
3. Две концентрации (B.5): поле "Общая концентрация" + поле "Концентрация NPK"
4. Кнопки культур: `data-npk-crop`, класс `.npk-crop-btn`
5. Кнопки фаз: `data-npk-phase`, класс `.npk-phase-btn`
6. Таблица: 4 колонки (Удобрение, NPK, Соответствие, Примечания)
7. Прогресс-бары: пустые div-контейнеры

⛔ ЗАПРЕТЫ:
❌ [Любая логика расчёта] — НЕТ `+ - * / %` в PHP-тегах
❌ [Вызов любых классов] — НЕТ `::`, `->`, `new`
❌ [Инлайн-стили] — НЕТ атрибута `style=`
❌ [Неправильные data-атрибуты] — ТОЛЬКО `data-npk-*`

🧪 ЧЕК-ЛИСТ:
[ ] 0 PHP-операторов вычислений
[ ] 0 вызовов методов классов
[ ] 0 атрибутов `style`
[ ] Все data-атрибуты с префиксом `data-npk-`
[ ] КТ-8: 0 PHP-логики расчёта

═══════════════════════════════════════════════════════════════════════════════

═══════════════════════════════════════════════════════════════════════════════
ЗАДАНИЕ №8: selector.css — Централизованные стили
═══════════════════════════════════════════════════════════════════════════════

📁 ФАЙЛЫ:
   СОЗДАТЬ: [module-fertilizer-selector/assets/css/selector.css]

📋 ТРЕБОВАНИЯ:
1. **ВСЕ** статические стили — ТОЛЬКО здесь
2. **ВСЕ** селекторы с префиксом `.npk-`
3. 0 глобальных селекторов
4. Стили прогресс-баров: `.npk-progress-green`, `.npk-progress-orange`, `.npk-progress-grey`
5. Стили для двух полей концентрации (B.5)

⛔ ЗАПРЕТЫ:
❌ [Глобальные селекторы] — НЕТ `div`, `table`, `button` без `.npk-`
❌ [Инлайн-стили в JS/HTML] — все статические стили здесь

🧪 ЧЕК-ЛИСТ:
[ ] 0 селекторов без префикса `.npk-`
[ ] Классы цветов прогресс-баров
[ ] Адаптивность (медиа-запросы)
[ ] КТ-5: 0 глобальных селекторов

⚠️ ТРИГГЕР АУДИТА: CSS — централизация стилей, 0 инлайн

═══════════════════════════════════════════════════════════════════════════════

═══════════════════════════════════════════════════════════════════════════════
ЗАДАНИЕ №9: selector.js — UX/UI и Instant Preview (ФИНАЛ)
═══════════════════════════════════════════════════════════════════════════════

📁 ФАЙЛЫ:
   СОЗДАТЬ: [module-fertilizer-selector/assets/js/selector.js]

📋 ТРЕБОВАНИЯ:
1. Instant Preview (<100мс) — алгоритм Отиаи для UX
2. Фильтрация: ≥ порога (из `NPK_Selector_Data.constants`)
3. AJAX: FormData(), AbortController
4. UI: все обновления через `requestAnimationFrame()`
5. Цвета: добавление классов, НЕ прямые стили
6. Две концентрации (B.5): обновление NPK концентрации при смене удобрения

⛔ ЗАПРЕТЫ:
❌ [Неправильный AJAX] — FormData(), НЕ JSON, НЕ URLSearchParams
❌ [Отмена запросов] — AbortController ОБЯЗАТЕЛЕН
❌ [Моргание UI] — Все DOM-изменения через rAF
❌ [Статические стили] — НЕТ `.style.` (кроме width/height)
❌ [Игнорирование финала] — Preview заменяется ответом сервера

🧪 ЧЕК-ЛИСТ:
[ ] Instant Preview: нормализация → минимумы → сумма → ×100
[ ] Порог из констант: `NPK_Selector_Data.constants.min_match_percentage`
[ ] AJAX: `new FormData()`, `append('action', 'npk_fertilizer_select')`
[ ] AbortController: `if (controller) controller.abort();`
[ ] rAF: `requestAnimationFrame(() => { ... })`
[ ] Цвета: `classList.add('npk-progress-green')`, НЕ `.style.background`
[ ] КТ-3: Instant Preview <100мс

⚠️ ТРИГГЕР АУДИТА: JS — Instant Preview, AJAX, rAF, AbortController

═══════════════════════════════════════════════════════════════════════════════

// ============================================
// ДЕБАГ-ПРОВЕРКИ КООРДИНАТОРА (ЭТАЛОННЫЙ ПОРЯДОК)
// ============================================
// 
// 1. Все 9 файлов имеют задания:              [✅]
// 2. Порядок создания соответствует эталону:  [✅] ← ИСПРАВЛЕНО
//    №3 Cache_Manager создаётся ДО №4 Data_Adapter
//    №3 Cache_Manager создаётся ДО №5 AJAX_Handler
//    №4 Data_Adapter создаётся ПОСЛЕ Cache_Manager
//    №5 AJAX_Handler создаётся ПОСЛЕ Cache_Manager и Selection_Engine
//    №9 JS создаётся ПОСЛЕ всех PHP-компонентов
// 
// 3. Разделение ответственности соблюдено:    [✅]
// 4. Только разрешённые методы БД:            [✅]
// 5. Все обязательные элементы в заданиях:    [✅]
// 6. Запрет E04 (URLSearchParams/JSON):       [✅]
// 7. Запрет E10 (инлайн-стили):               [✅]
// 8. Согласованность интерфейсов:             [✅]
// 9. Префикс NPK_ для всех классов:           [✅] — ВСЕ классы с NPK_
// 10. Требование B.5 покрыто заданиями 7,8,9: [✅]
// 11. Вердикт строго бинарен:                 [✅]
// 12. НЕТ рекомендаций по исправлению:        [✅] — пакет готов
// 13. Все требования Дополняющего ТЗ покрыты: [✅]
// 
// ============================================
// ✅ ВСЕ ПРОВЕРКИ ПРОЙДЕНЫ
// ============================================

═══════════════════════════════════════════════════════════════════════════════
📦 ИТОГОВЫЙ ПАКЕТ ЗАДАНИЙ — ЭТАЛОННЫЙ ПОРЯДОК №1 → №9
═══════════════════════════════════════════════════════════════════════════════

✅ №1: fertilizer-selector-module.php
✅ №2: class-selection-engine.php — ядро, константы порога
✅ №3: class-cache-manager.php — кеш (ЗАВИСИМОСТЬ)
✅ №4: class-data-adapter.php — использует кеш №3
✅ №5: class-ajax-handler.php — использует кеш №3 + ядро №2
✅ №6: class-shortcode.php — использует адаптер №4
✅ №7: selector-template.php — HTML
✅ №8: selector.css — стили
✅ №9: selector.js — UX, AJAX к №5

⚠️ ТРИГГЕРЫ АУДИТА АКТИВИРОВАНЫ:
   • №2 — Алгоритм Отиаи + Константы порога
   • №3 — Cache_Manager (новый компонент)
   • №5 — AJAX (FormData, запрет JSON, кеш)
   • №8 — CSS (0 глобальных, 0 инлайн)
   • №9 — JS (Instant Preview, AbortController, rAF)

═══════════════════════════════════════════════════════════════════════════════
🚨 КООРДИНАТОР: ПАКЕТ ГОТОВ К ПЕРЕДАЧЕ КОДЕРУ
═══════════════════════════════════════════════════════════════════════════════

ПОРЯДОК ПЕРЕДАЧИ КОДЕРУ: СТРОГО №1 → №2 → №3 → №4 → №5 → №6 → №7 → №8 → №9
ЗАВИСИМОСТИ ЗАМКНУТЫ: ✅
ФАТАЛЬНЫХ ОШИБОК PHP 500: 0
ИСТОРИЧЕСКИХ ОШИБОК: 0

═══════════════════════════════════════════════════════════════════════════════