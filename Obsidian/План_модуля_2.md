---
tags:
  - Ledgreen_ru
  - Калькулятор
  - AI
  - Подбор_удобрений
Похожее:
Сфера:
  - "[[Бизнес]]"
  - "[[IT]]"
  - "[[Web]]"
Источник:
Ссылки:
  - "[[Полезное]]"
  - "[[Комплексный калькулятор удобрений]]"
  - "[[Подбор удобрений]]"
  - "[[Калькулятор концентрации]]"
  - "[[Схемы поэтапного создания плагина]]"
Категория:
Дата:
---
Отлично! Вижу, что верификатор принял план с незначительными замечаниями. Это хороший результат.

Давай на основе этих замечаний подготовлю **финальную, уточненю версию плана**, которая будет готова к передаче в разработку (Чату 2: Дизайнер).

Я интегрирую все рекомендации верификатора:

1.  **Уточню структуру БД:** В Этапе 1 явно укажу, что таблица связей `crop_phase_fertilizer` уже существует в БД версии 4.0, и мы будем использовать её, а не создавать новую.
2.  **Предложу оптимизацию загрузки фаз:** В Этапе 4 добавлю альтернативный, более производительный вариант с передачей фаз через `data-атрибут`, оставив выбор за разработчиком.
3.  **Добавлю обработку пустых результатов:** В Этап 6 внесу требование рендерить понятное сообщение, если удобрения не найдены.
4.  **Усилю требования к безопасности:** В Этапе 6 уточню использование `esc_attr()` наряду с `esc_html()`.
5.  **Детализирую тестирование:** В Этап 8 добавлю пункт о тестировании сценария с отключенным модулем 1.

Вот финальная версия плана, готовая к реализации:

---

**ПЛАН: Разработка модуля подбора удобрений**
**ТИП: 2 (Подбор/выбор)**
**УРОВЕНЬ: ВЫСОКИЙ**

### **ЭТАП 1: Фундамент модуля и интеграция с ядром**
*   **ФАЙЛЫ:**
    *   `module-fertilizer-selector/fertilizer-selector-module.php`
    *   `module-fertilizer-selector/includes/class-cache-manager.php`
*   **ЗАДАЧА:**
    1.  Создать главный файл модуля. Перед регистрацией проверить существование класса `NPK_Calculators_Core`. Если класс отсутствует — не инициализировать модуль и вывести `admin_notice` с требованием активировать основной плагин.
    2.  Создать класс `NPK_Selector_Cache_Manager` с методами `set($key, $data, $expire=3600)`, `get($key)`, `invalidate($crop_id=null)`. Ключи формировать как `'npk_selector_' . md5($crop_id . '_' . $phase_id)`.
    3.  Добавить автозагрузку классов модуля через `spl_autoload_register` с префиксом `NPK_Selector_`.
    4.  **Важно:** Использовать существующую таблицу связей `crop_phase_fertilizer` из БД версии 4.0. Не создавать новую.
*   **КРИТЕРИЙ:**
    *   При отсутствии ядра плагина модуль не вызывает фатальных ошибок, показывает понятное уведомление в админке.
    *   Кеш работает, данные не запрашиваются из БД чаще раза в час для одинаковых параметров.
    *   Все классы модуля имеют уникальный префикс `NPK_Selector_`.
*   **РИСКИ:**
    *   ❌ Нарушение порядка загрузки (Ошибка 3)
    *   ❌ Неправильные имена файлов (Ошибка 1)
    *   ❌ Отсутствие префиксов в классах (конфликты с другими плагинами)

---

### **ЭТАП 2: Ядро алгоритма подбора (Отиаи)**
*   **ФАЙЛЫ:**
    *   `module-fertilizer-selector/includes/class-selection-engine.php`
*   **ЗАДАЧА:**
    1.  Реализовать метод `find_matching_fertilizers($crop_id, $phase_id)`. В начале метода выполнить проверку `class_exists('NPK_Database')` перед использованием `NPK_Database::normalize_npk()`.
    2.  Реализовать метод `calculate_match_percentage($target_npk, $fertilizer_npk): float` на основе модифицированного евклидова расстояния с весовыми коэффициентами: N=1.0, P=1.2, K=1.1.
    3.  Реализовать fallback-режим: если максимальный процент соответствия среди всех удобрений ниже порога (значение из настроек, по умолчанию 75%), возвращать топ-3 лучших варианта с флагом `'fallback' => true`.
    4.  Генерация подсказок (`tips`): находить пересечение полей `additives` целевой культуры/фазы и `priority_additives` удобрения.
*   **КРИТЕРИЙ:**
    *   Азофоска (16:16:16) для томата на фазе рассады (N=1, P=2, K=1) дает процент соответствия строго ~83.7% с учетом весов.
    *   Все входящие NPK значения перед расчетом проходят через `NPK_Database::normalize_npk()`.
    *   Метод возвращает массив, каждый элемент которого содержит поля: `id`, `name`, `match_percentage`, `fallback` (bool), `tips` (array).
*   **РИСКИ:**
    *   ❌ Дублирование алгоритма вне этого класса (запрет ТЗ)
    *   ❌ Отсутствие проверки существования `NPK_Database`
    *   ❌ Несоответствие весов коэффициентов агрономическим требованиям

---

### **ЭТАП 3: Базовая структура UI и рендеринг формы**
*   **ФАЙЛЫ:**
    *   `module-fertilizer-selector/includes/class-form-renderer.php`
    *   `assets/css/module-selector-base.css`
*   **ЗАДАЧА:**
    1.  Создать класс `NPK_Selector_Form_Renderer` с методом `render_form($atts)`, обрабатывающим атрибуты шорткода `[fertilizer_selector crop_id="X" theme="light"]`.
    2.  Метод `render_form()` выводит HTML-форму с двумя селектами: для культур (заполняется из таблицы `crops`) и для фаз (пустой, заполняется динамически).
    3.  CSS оформить по БЭМ: `.npk-selector`, `.npk-selector__select`, `.npk-selector__results` и т.д. Все стили должны быть изолированы и не влиять на глобальные.
    4.  В форму добавить скрытый контейнер для спиннера и data-атрибуты с `ajaxurl`, `nonce`, и начальным `crop_id` (если был передан).
*   **КРИТЕРИЙ:**
    *   Шорткод выводит форму, не ломая верстку темы.
    *   CSS использует только классы с префиксом `npk-selector-`.
    *   Все необходимые данные для AJAX переданы через data-атрибуты.
*   **РИСКИ:**
    *   ❌ Глобальные стили, влияющие на всю тему (Ошибка 5)
    *   ❌ Использование ID вместо классов в селекторах CSS/JS

---

### **ЭТАП 4: JavaScript для Instant Preview (с оптимизацией)**
*   **ФАЙЛЫ:**
    *   `assets/js/module-selector.js`
*   **ЗАДАЧА:**
    1.  Создать JS-модуль, который при загрузке страницы инициализирует все контейнеры `.npk-selector`.
    2.  Реализовать загрузку фаз. **Вариант А (оптимальный):** При рендеринге формы передавать все фазы в JSON-формате в скрытом data-атрибуте. **Вариант Б (простой):** При изменении `select.npk-selector__crop` отправлять AJAX-запрос (FormData) на `admin-ajax.php` с действием `npk_get_phases` для получения и заполнения селекта фаз.
    3.  При изменении любого из селектов запускать поиск удобрений с **debounce (задержка 300 мс)**, отменяя предыдущий запрос:
        *   Создать `FormData`, добавить `action='npk_get_fertilizers'`, `crop_id`, `phase_id`, `nonce`.
        *   Отправить через `fetch()`, не указывая `Content-Type` (браузер выставит `multipart/form-data`).
        *   При успехе заменить содержимое `.npk-selector__results` полученным HTML.
    4.  Реализовать состояния: "загрузка" (показать спиннер), "ошибка", "пустой результат".
*   **КРИТЕРИЙ:**
    *   При быстром переключении селектов отправляется только последний запрос.
    *   Данные отправляются через FormData, а не JSON (Ошибка 2).
    *   JS не содержит жестких ID-селекторов, только классы.
*   **РИСКИ:**
    *   ❌ Множественные запросы из-за отсутствия debounce
    *   ❌ Зависимость от глобального jQuery

---

### **ЭТАП 5: AJAX-обработчики на сервере**
*   **ФАЙЛЫ:**
    *   `module-fertilizer-selector/includes/class-ajax-handlers.php`
*   **ЗАДАЧА:**
    1.  Создать класс `NPK_Selector_Ajax_Handlers` с методами `register_hooks()`, `handle_get_phases()`, `handle_get_fertilizers()`.
    2.  В `handle_get_phases`: проверить nonce, получить `crop_id`, загрузить фазы через `NPK_Database::get_phases_by_crop()`, вернуть JSON с HTML-опциями.
    3.  В `handle_get_fertilizers`:
        *   Проверить nonce, а также `class_exists('NPK_Selector_Selection_Engine')` и `class_exists('NPK_Selector_Results_Renderer')`.
        *   Получить `crop_id`, `phase_id`, проверить наличие данных в `NPK_Selector_Cache_Manager::get()`.
        *   При кеш-промахе: создать экземпляр `Selection_Engine`, получить результаты, сохранить в кеш.
        *   Создать экземпляр `Results_Renderer`, вызвать `render_cards()`, вернуть готовый HTML в JSON-ответе.
*   **КРИТЕРИЙ:**
    *   При отсутствии необходимых классов возвращается понятная ошибка в консоль/пользователю.
    *   Nonce проверяется для всех защищенных действий.
    *   Кеш используется до обращения к тяжелому движку подбора.
*   **РИСКИ:**
    *   ❌ Отсутствие проверок существования классов (Ошибка 4)
    *   ❌ Генерация HTML вне класса `Results_Renderer`

---

### **ЭТАП 6: Визуализация карточек результатов**
*   **ФАЙЛЫ:**
    *   `module-fertilizer-selector/includes/class-results-renderer.php`
    *   `assets/css/module-selector-results.css`
*   **ЗАДАЧА:**
    1.  Создать класс `NPK_Selector_Results_Renderer` с методом `render_cards($fertilizers, $theme)`, который возвращает строку с HTML-карточками.
    2.  Карточка должна содержать: название удобрения, процент соответствия (числом и цветным прогресс-баром), список подсказок (чипсы).
    3.  **Обязательно:** Использовать `esc_html()` для текста и `esc_attr()` для атрибутов. Все стили — только в CSS-файле (никаких инлайн-стилей).
    4.  Если массив `$fertilizers` пуст, возвращать сообщение: "Нет удобрений, соответствующих выбранным параметрам".
    5.  Если `$fertilizers['fallback']` === true, перед карточками вывести уведомление "Подобраны ближайшие аналоги".
*   **КРИТЕРИЙ:**
    *   HTML генерируется строго в этом классе, не дублируется.
    *   Процент соответствия влияет на цвет/ширину прогресс-бара через CSS-классы.
    *   Нет XSS-уязвимостей благодаря экранированию.
*   **РИСКИ:**
    *   ❌ Инлайн-стили в PHP (нарушение ТЗ)
    *   ❌ XSS-уязвимости из-за отсутствия экранирования

---

### **ЭТАП 7: Административные настройки**
*   **ФАЙЛЫ:**
    *   `module-fertilizer-selector/includes/class-admin-settings.php`
    *   `assets/js/module-selector-admin.js`
*   **ЗАДАЧА:**
    1.  Создать класс `NPK_Selector_Admin_Settings` для добавления страницы настроек в меню "NPK Calculators".
    2.  Зарегистрировать настройки с уникальным префиксом `npk_selector_`: порог fallback (int 0-100), время жизни кеша (select), весовые коэффициенты N,P,K (float).
    3.  Реализовать методы санитизации и валидации (`sanitize_settings()`) для всех входящих данных перед сохранением.
    4.  Добавить JS для улучшения UX (ползунок для порога, подсказки).
*   **КРИТЕРИЙ:**
    *   Изменение настроек влияет на работу `Selection_Engine` и `Cache_Manager`.
    *   Все данные проходят санитизацию.
    *   Интерфейс адаптивный, в стиле WordPress.
*   **РИСКИ:**
    *   ❌ Сохранение несанитизированных данных (XSS)
    *   ❌ Конфликт имен опций (использован уникальный префикс)

---

### **ЭТАП 8: Интеграционное тестирование и оптимизация**
*   **ФАЙЛЫ:**
    *   `module-fertilizer-selector/fertilizer-selector-module.php` (финальные правки)
    *   `tests/test-selection-engine.php`
*   **ЗАДАЧА:**
    1.  Добавить фильтры для расширяемости: `npk_selector_modify_results`, `npk_selector_cache_expire`.
    2.  **Интеграционный тест:** Активировать модуль 2 при отключенном модуле 1 — убедиться, что появляется корректное уведомление, а не фатальная ошибка.
    3.  **Функциональный тест:** Создать тестовый набор из 10 удобрений, проверить правильность ранжирования и работы fallback-логики.
    4.  **Оптимизация БД:** Убедиться, что в таблице `crop_phase_fertilizer` есть составной индекс по полям `crop_id` и `phase_id` для быстрых выборок.
*   **КРИТЕРИЙ:**
    *   Модули 1 и 2 работают совместно без конфликтов.
    *   Все методы используют `NPK_Database::normalize_npk()` перед расчетами.
    *   Время выполнения AJAX-запроса с закешированным результатом < 100 мс, без кеша < 300 мс.
*   **РИСКИ:**
    *   ❌ Забыли применить `normalize_npk()` (Ошибка 6)
    *   ❌ Медленные запросы к БД из-за отсутствия индексов